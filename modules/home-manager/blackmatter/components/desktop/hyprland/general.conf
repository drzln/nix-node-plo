################################################################################
# basic navigation
################################################################################

bind = Super, Return, exec, $terminal
bind = Super, Q, closewindow
bind = Super, R, exec, $menu

# end basic navigation
